# Branch Strategy

The repository now uses the `main` branch as the canonical branch. All active work has been
consolidated into `main`, and the previous working branch has been removed to avoid confusion.
